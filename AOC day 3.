		ArrayList<String> a =new ArrayList<>();
		a=AOCSTRING.StringToArray(c,'\n');
		long n=0;
		for (String l:a) {
			long t=0;
			int p=-1;
			for (int i =11;i>=0;i--) {
				int j=p+1;
				for (int k=p+1;k<l.length()-i;k++) {
					if (l.charAt(k)>l.charAt(j))
						j=k;
				}
				p=j;
				t+=Math.pow(10, i)*(l.charAt(j)-'0');
			}
			
				System.out.println(t+" n: "+n);
				n+=t;
				
		}	
		System.out.println(n);
	}
